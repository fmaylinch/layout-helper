
# If you change something here just run "pod install" from the command line.
#
# Don't install CocoaPods gem from AppCode even when AppCode tells you to do it. It breaks the gem.
# If CocoaPods gem breaks: gem uninstall cocoapods (and clean everything), gem install cocoapods.
# If it doesn't work, reinstall "xcodeproj" gem first and then install the CocoaPods gem.
#
# You can see available versions here: https://github.com/CocoaPods/Specs/tree/master/Specs/<POD_NAME>

platform :ios, '9.1'
pod 'IQDropDownTextField', '1.0.7'
pod 'NMRangeSlider', '1.2.1'
